{{ form_start(form, {'attr': {'action' : '/send-us-a-message', 'class' : 'php-email-form'}}) }}

    <div class="col-md-12">
        <h3> 
            <i class="bx bx-message text-danger"></i>
            Veuillez complèter le formulaire ci-dessous
        </h3><hr>
    </div>

    <div class="row">
        <div class="col-md">
            {{ form_row(form.email) }}
        </div>
    </div>

    <div class="row">
        <div class="col-md">{{ form_row(form.name) }}</div>
        <div class="col-md">{{ form_row(form.subject) }}</div>
    </div>
    
{{ form_widget(form) }}
<div class="text-center"><button type="submit" class="btn btn--red"><i class="fa fa-sent"></i> Envoyez-nous</button></div>
{{ form_end(form) }}