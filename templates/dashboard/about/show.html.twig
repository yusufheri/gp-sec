{% extends 'base.html.twig' %}

{% block title %}About{% endblock %}

{% block body %}
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about.id }}</td>
            </tr>
            <tr>
                <th>FirstPicture</th>
                <td>{{ about.firstPicture }}</td>
            </tr>
            <tr>
                <th>SecondPicture</th>
                <td>{{ about.secondPicture }}</td>
            </tr>
            <tr>
                <th>Firsttext</th>
                <td>{{ about.firsttext }}</td>
            </tr>
            <tr>
                <th>SecondText</th>
                <td>{{ about.secondText }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ about.updatedAt ? about.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dashboard_about_index') }}">back to list</a>

    <a href="{{ path('app_dashboard_about_edit', {'id': about.id}) }}">edit</a>

    {{ include('dashboard/about/_delete_form.html.twig') }}
{% endblock %}
