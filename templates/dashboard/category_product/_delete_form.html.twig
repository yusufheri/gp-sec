<form method="post" action="{{ path('app_dashboard_category_product_delete', {'id': category_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_product.id) }}">
    <button class="btn btn--white u-margin-top-small u-margin-bottom-small"><i class="fa fa-trash"></i> Delete</button>
</form>
