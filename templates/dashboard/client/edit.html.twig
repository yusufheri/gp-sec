{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}Modifier Client | GP-SEC{% endblock %}

{% block dashboard_body %}
<div class="dashboard-header">
    <h1>Modifier le Client</h1>
    <p>Mise à jour pour : <strong>{{ client.name }}</strong></p>
</div>

<div class="gp-card">
    {{ include('dashboard/client/_form.html.twig') }}
    
    <div class="mt-4 d-flex justify-content-between">
        <a href="{{ path('app_dashboard_client_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
        {{ include('dashboard/client/_delete_form.html.twig') }}
    </div>
{% endblock %}
