<form method="post" action="{{ path('app_dashboard_image_product_delete', {'id': image_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_product.id) }}">
    <button class="btn">Delete</button>
</form>
