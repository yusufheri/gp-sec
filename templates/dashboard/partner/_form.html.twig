{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-danger mt-3"><i class="bi bi-check-circle me-1"></i> {{ button_label|default('Enregistrer le partenaire') }}</button>
{{ form_end(form) }}
