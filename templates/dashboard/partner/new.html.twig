{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}Nouveau Partenaire | GP-SEC{% endblock %}

{% block dashboard_body %}
<div class="dashboard-header">
    <h1>Ajouter un Partenaire</h1>
    <p>Enregistrez un nouveau partenaire pour l'afficher sur le site.</p>
</div>

<div class="gp-card">
    {{ include('dashboard/partner/_form.html.twig') }}
    
    <div class="mt-4">
        <a href="{{ path('app_dashboard_partner_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
