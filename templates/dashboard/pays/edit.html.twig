{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}Modifier Pays | GP-SEC{% endblock %}

{% block dashboard_body %}
<div class="dashboard-header">
    <h1>Modifier le Pays</h1>
    <p>Mise à jour pour : <strong>{{ pay.name }}</strong></p>
</div>

<div class="gp-card">
    {{ include('dashboard/pays/_form.html.twig') }}
    
    <div class="mt-4 d-flex justify-content-between">
        <a href="{{ path('app_dashboard_pays_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
        {{ include('dashboard/pays/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
