{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pay.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pay.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dashboard_pays_index') }}">back to list</a>

    <a href="{{ path('app_dashboard_pays_edit', {'id': pay.id}) }}">edit</a>

    {{ include('dashboard/pays/_delete_form.html.twig') }}
{% endblock %}
