{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}Nouveau Service | GP-SEC{% endblock %}

{% block dashboard_body %}
<div class="dashboard-header">
    <h1>Ajouter un Service</h1>
    <p>Définissez un nouveau service pour vos clients.</p>
</div>

<div class="gp-card">
    {{ include('dashboard/service/_form.html.twig') }}
    
    <div class="mt-4">
        <a href="{{ path('app_dashboard_service_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
