{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn--red u-margin-bottom-small"><i class="fa fa-plus"></i> {{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
