{% extends 'base.html.twig' %}

{% block title %}Edit Testimonial{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md">
                <h1>Edit Testimonial</h1>

                {{ include('dashboard/testimonial/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_dashboard_testimonial_index') }}">back to list</a>

                {{ include('dashboard/testimonial/_delete_form.html.twig') }}            
            </div>
        </div>
    </div>
{% endblock %}
