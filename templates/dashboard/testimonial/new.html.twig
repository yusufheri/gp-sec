{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}Nouveau Témoignage | GP-SEC{% endblock %}

{% block dashboard_body %}
<div class="dashboard-header">
    <h1>Ajouter un Témoignage</h1>
    <p>Enregistrez un nouvel avis client pour le site.</p>
</div>

<div class="gp-card">
    {{ include('dashboard/testimonial/_form.html.twig') }}
    
    <div class="mt-4">
        <a href="{{ path('app_dashboard_testimonial_index') }}" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
